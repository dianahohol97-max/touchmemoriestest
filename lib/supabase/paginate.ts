/**
 * Усі рядки запиту, а не перша тисяча.
 *
 * PostgREST віддає щонайбільше тисячу рядків і не каже про це нічим: помилки
 * немає, у журнал не потрапляє ніщо, відповідь просто коротша за правду. За
 * 14.09.2026 ця пастка спрацювала чотири рази — список клієнтів, дашборд,
 * аналітика на довгому періоді й один діалог у скриньці, — і щоразу її
 * помічали випадково, уже після того, як хтось подивився на неправильне число.
 *
 * Правило, через яке цей файл існує, — гоча 14 в CLAUDE.md: читання з великих
 * таблиць пишеться з пагінацією ОДРАЗУ, незалежно від сьогоднішнього розміру
 * таблиці. Один помічник замість циклу, переписаного вкотре: кожна власна
 * копія — це ще одне місце, де хтось забуде `.order()` і сторінки почнуть
 * тасуватися.
 */

/** Скільки рядків PostgREST віддає за один запит. */
export const PAGE_SIZE = 1000;

/**
 * Збирає всі сторінки відповіді.
 *
 * `build` отримує межі рядків і має повернути ЗАПИТ ІЗ СОРТУВАННЯМ. Без
 * `.order()` база не зобовʼязана віддавати сторінки в сталому порядку, і рядки
 * почнуть повторюватися й зникати між ними — помилка, яку видно ще гірше, ніж
 * просту нестачу.
 *
 * Помилка НЕ ковтається: функція кидає. Повернути те, що встигли зібрати,
 * означало б підмінити коротку відповідь ще коротшою — рівно та тиха неправда,
 * проти якої все це написано.
 */
export async function fetchAllRows<T>(
    build: (from: number, to: number) => PromiseLike<{ data: T[] | null; error: any }>,
    options: { pageSize?: number; label?: string } = {},
): Promise<T[]> {
    const pageSize = options.pageSize ?? PAGE_SIZE;
    const rows: T[] = [];

    for (let from = 0; ; from += pageSize) {
        const { data, error } = await build(from, from + pageSize - 1);
        if (error) {
            const where = options.label ? ` (${options.label})` : '';
            throw new Error(`Не вдалося прочитати сторінку даних${where}: ${error.message || error}`);
        }
        rows.push(...((data || []) as T[]));
        if (!data || data.length < pageSize) break;
    }
    return rows;
}
