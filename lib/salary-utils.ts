export async function calculateSalary(staffId: string, periodStart: string, periodEnd: string) {
    // TODO — salary formula will be configured per staff role.

    // Default fallback values
    return {
        base_amount: 0,
        bonus_amount: 0,
        total_amount: 0
    };
}
