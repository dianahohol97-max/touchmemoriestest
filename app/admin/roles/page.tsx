import { redirect } from 'next/navigation';

/**
 * Стара сторінка ролей.
 *
 * Показувала шість галочок і перекладала їх у рівні доступу туди й назад.
 * Переклад був втратним у двох напрямках: редагувати можна було лише шість
 * розділів із десяти, а «Може управляти продуктами» завжди повертало
 * catalog: 'edit', тож роль власника з catalog: 'full' після першого ж
 * «Зберегти» тихо ставала слабшою. Те саме з аналітикою, контентом і
 * фінансами, де переклад умів лише 'view'.
 *
 * Повноцінний редактор увесь цей час лежав поруч, за адресою
 * /admin/settings/team/roles: десять розділів, чотири рівні, без перекладу.
 * Меню тепер веде туди, а ця адреса лишається як перенаправлення, щоб не
 * ламати збережені посилання.
 */
export default function LegacyRolesRedirect() {
    redirect('/admin/settings/team/roles');
}
